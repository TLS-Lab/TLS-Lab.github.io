Add real assets here as they become available:

- `profile.jpg` — headshot, referenced (commented) on `index.qmd` / `about.qmd` to
  replace the CSS `.avatar-placeholder`
- `logo.png` — optional navbar logo
- `favicon.png` — optional browser tab icon
